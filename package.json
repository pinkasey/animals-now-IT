{
  "name": "animals-now-it.git",
  "version": "1.0.0",
  "description": "Integration Tests for Animals-Now website",
  "main": "index.js",
  "scripts": {
    "cleandir": "cleandir ./allure-results",
    "test": "npm run cleandir && protractor conf-animals.js",
    "test-failure": "npm run cleandir && protractor conf-animals-failure.js",
    "sanity": "npm run cleandir && protractor conf-animals-sanity.js",
    "webdriver-update": "webdriver-manager update",
    "webdriver-start": "webdriver-manager start --detach",
    "webdriver-start-attached": "webdriver-manager start",
    "webdriver-stop": "webdriver-manager shutdown",
    "webdriver-version": "webdriver-manager version",
    "webdriver-set-chrome-version": "webdriver-manager update --versions.chrome 74.0.3729.169"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pinkasey/animals-now-IT.git"
  },
  "author": "eyal@animals-now.org",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/pinkasey/animals-now-IT/issues"
  },
  "homepage": "https://github.com/pinkasey/animals-now-IT#readme",
  "dependencies": {
    "jasmine-allure-reporter": "^1.0.2",
    "protractor": "^5.4.2",
    "query-string": "^6.8.1"
  },
  "devDependencies": {
    "clean-dir": "^1.0.3"
  }
}
